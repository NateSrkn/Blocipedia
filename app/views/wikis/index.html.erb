<h1>All Wikis</h1>

<% if policy(Wiki).new? %>
    <h2><%= link_to 'New', new_wiki_path, class: 'link' %></h2>
<% end %>

<ul>
    <% @wikis.each do |wiki| %>
        <li><%=link_to wiki.title, wiki %></li>
    <% end %>
</ul>
